# Tidewave

See `examples/` folder.

## Development

### Requirements

- Python 3.8+
- [uv](https://docs.astral.sh/uv/) (recommended)

### Install with uv

```bash
# Install
uv sync

# Run tests
uv run pytest

# Lint and format code
uv run ruff check --fix .
uv run ruff format .
```
